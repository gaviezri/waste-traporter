from constants import CREDENTIALS_PATH
from modules.scale import Driver as ScaleDriver
from modules.sharepoint import SharePointDriver
from modules.types import WasteType
import json

def main():
    # scale_driver = ScaleDriver()
    # print(scale_driver.read_stable_weight_kg())
    # reporter = SharePointManager(json.load(open(CREDENTIALS_PATH)))
    while True:
        pass
    

main()
